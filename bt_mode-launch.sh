#!/bin/sh
# Launches the BT Mode splash app. Suitable for the stock Anbernic
# launcher to invoke directly (it scans for .sh files in APP dirs).
exec /usr/bin/python3 /usr/local/slothos/bt_mode/bt_mode.py
